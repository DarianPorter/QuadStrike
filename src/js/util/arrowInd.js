export default class Ind {
    constructor(colors){
        this.colors = colors;
        // find all 4 keys
    }
    keypress(keycode){
        switch(keycode){
            case 00:

                break;
            case 00:

                break;
            case 00:

                break;
            case 00:

                break;
        }
    }
    changeStyleWithException(exception){
        for(let i = 0; i < 4; ){
            if(i !== exception){
                // this.keys.style.shadow = none
            }else(
                //key[]
            )
        }
    }
}